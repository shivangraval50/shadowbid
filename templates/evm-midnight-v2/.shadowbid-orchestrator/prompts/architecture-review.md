# Mission

Perform a compact, read-only architecture and privacy review for ShadowBid before implementation. Read AGENTS.md and the concise files listed there, then inspect only the template code needed to ground every recommendation in real EVM, Midnight Compact, EffectStream, batcher, and frontend APIs.

# Deliverable

Return a concise implementation blueprint containing:

- exact existing packages/files and extension points;
- a technically honest sealed-bid privacy model and public/private data table;
- EVM auction/NFT escrow and settlement authorization design;
- Compact state, witness/private-state, circuit, and generated-binding approach supported by this pinned SDK/compiler;
- EffectStream deterministic combined-state and duplicate/replay handling;
- batcher settlement validation boundary;
- stable cross-package interfaces and ordered milestones;
- critical risks, trust assumptions, and tests that prove claims.

Do not edit files. Do not invent APIs, proof verification, bridge behavior, or privacy guarantees. Spend words on decisions that prevent expensive implementation mistakes.
