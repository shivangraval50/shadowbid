# Agent handoff

Last reviewed: 2026-08-30 (shadowbid/claude-settlement).

Start from [`SETUP_STATUS.md`](SETUP_STATUS.md), whose reference-stack facts are preserved. This update was scoped to closing the operational-settlement-path blockers `SOL_FINAL_REVIEW.md` and `SOL_REVIEW_1.md` raised, entirely inside `packages/batcher/`. No Compact, Solidity, EffectStream primitive/reducer, API, or frontend files were changed.

Completed: `packages/batcher/shadowbid-coordinator.ts` authenticates an off-chain coordinator result via EIP-712 (same digest/domain as `ShadowBidAuction.settle`), cross-checks it against injected Midnight ledger state, and only then reports `SETTLEMENT_READY`. `shadowbid-settlement.ts`'s `validateInput` now actually runs that check instead of unconditionally throwing. `shadowbid-coordinator-wiring.ts` builds the real reader from environment variables in both batcher entrypoints, falling back to the pre-existing fail-closed reader if unconfigured. 23/23 batcher+node ShadowBid unit tests pass (`bun test packages/batcher/shadowbid-settlement.test.ts packages/batcher/shadowbid-coordinator.test.ts packages/node/shadowbid.test.ts`), and the Compact privacy-boundary vitest suite still passes 4/4 confirming the circuit set is unchanged (8 circuits, no `publish_coordinator_result`). See docs/CLAUDE_SETTLEMENT_REPORT.md for full command output.

Known blockers (all pre-existing, not introduced by this change):

- No live Midnight indexer/node connection was reachable from this environment, so no real `MidnightAuctionStateReader` is wired into either batcher entrypoint — both remain fail-closed even with the new environment variables set. This is the actual remaining work: implement a `MidnightAuctionStateReader` backed by `@midnight-ntwrk/midnight-js-indexer-public-data-provider` (or equivalent) that decodes `ShadowBidContract.ledger(state)` and adapts it with the provided `toHexLedgerState` helper.
- A real coordinator process (watches the Midnight commit deadline, decides the winner out-of-band from private bid data it has separate access to, holds `settlementSigner`'s key, signs a `CoordinatorResult`, writes it via `FileCoordinatorResultStore`) does not exist and was intentionally not built — that process is where all trust in this system concentrates, and needs its own dedicated review before existing anywhere near production key material.
- `contract-shadowbid`'s Compact compile still uses `--skip-zk`; a full proving-key generation was attempted once and did not finish in 90s in this environment (unlike the trivial Counter reference contract, which does complete). No proof-capable settlement run has been executed; do not claim one has.
- `bun run test` (full orchestrated suite) and `bun run --cwd packages/frontend build` both still fail here on pre-existing environment gaps unrelated to this change (missing `@nomicfoundation/hardhat-ignition/modules` during deploy; frontend needs a deployed local chain's address manifest). See BUILD_STATUS.md for exact errors.

Next action: implement the `MidnightAuctionStateReader` against a real indexer connection, build and vet the out-of-scope coordinator process against its own security review, then validate one real end-to-end settlement (Midnight commit → close → coordinator signs → batcher accepts → winner calls `settle()` on EVM) on a host that can run the full local stack and generate Compact proving keys.
